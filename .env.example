# ---------------------------------------------------------------------------
# CLIENT (VITE_ prefixed) — these are bundled into the JS every visitor
# downloads. Never put a secret here.
# ---------------------------------------------------------------------------
VITE_SUPABASE_URL=https://otbirgxzathmwinzhznd.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_QFTi2Kbd_6VSEzSIYKyqwA_g2uumah0
VITE_CONTACT_PHONE=917017138349

# Google Analytics 4 measurement ID (G-XXXXXXXXXX).
# Leave empty to ship with analytics and the consent banner fully disabled.
VITE_GA_ID=

# ---------------------------------------------------------------------------
# SERVER — used only by api/admin.ts. No VITE_ prefix, so these are never
# exposed to the browser. VITE_ADMIN_PASSWORD is gone on purpose: it used to
# ship in the client bundle in plaintext.
# ---------------------------------------------------------------------------
ADMIN_PASSWORD=change-me
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_URL=https://otbirgxzathmwinzhznd.supabase.co
